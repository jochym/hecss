name: Deploy to GitHub Pages
on:
  push:
    branches: [ "devel" ] # [ "main", "master" ]
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps: [uses: fastai/workflows/quarto-ghp@master]
    # steps: [uses: jochym/hecss/actions/quarto@devel]
